fpath+=~/.zfunc
if [ "$TMUX" = "" ]; then tmux source-file ~/.tmux.conf; tmux; fi

autoload -U +X bashcompinit && bashcompinit
autoload -U +X compinit && compinit

source ~/.zshrc.base
typeset -g POWERLEVEL9K_INSTANT_PROMPT=off
source ~/.aliases
